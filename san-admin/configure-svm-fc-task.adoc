---
permalink: san-admin/configure-svm-fc-task.html 
sidebar: sidebar 
keywords: configure, svm, for fc 
summary: Per configurare una macchina virtuale di archiviazione per FC, è necessario creare LIF per la SVM e assegnare il protocollo FC a tali LIF. 
---
= Configurare una SVM per FC
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
Per configurare una SVM (Storage Virtual Machine) per FC, è necessario creare LIF per SVM e assegnare il protocollo FC a tali LIF.

.A proposito di questa attività
NetApp supporta almeno un LIF FC per nodo per ogni SVM che fornisce dati con il protocollo FC. È necessario utilizzare due LIF per nodo e due fabric, con un LIF per nodo collegato. Ciò garantisce la ridondanza a livello di nodo e fabric.

.Prima di iniziare
* Per eseguire questa attività, è necessario essere un amministratore del cluster.
* È necessario disporre di una licenza FC (link:../system-admin/manage-licenses-concept.html#licenses-included-with-ontap-one["Incluso con ONTAP One"]) e deve essere attivata. In caso contrario, la licenza FC non è abilitata, le LIF e le SVM appariranno online, ma lo stato operativo sarà `down`. Il servizio FC deve essere abilitato affinché i tuoi LIF e SVM siano operativi. Per ospitare gli iniziatori, è necessario utilizzare lo zoning initiator singolo per tutte le LIF FC nella SVM.


.Fasi
[role="tabbed-block"]
====
.System Manager
--
Configurazione di una VM di storage per iSCSI con Gestore di sistema di ONTAP (9.7 e versioni successive).

[cols="2"]
|===
| Per configurare FC su una nuova VM di storage | Per configurare FC su una VM di storage esistente 


 a| 
. In System Manager, selezionare *Storage > Storage VM* e quindi selezionare *Add*.
. Immettere un nome per la VM di storage.
. Selezionare *FC* per il protocollo di accesso*.
. Seleziona *Abilita FC*.
+
Le porte FC vengono assegnate automaticamente.

. Selezionare *Salva*.

 a| 
. In System Manager, selezionare *Storage > Storage VM*.
. Seleziona la macchina virtuale di archiviazione che desideri configurare.
. Seleziona la scheda *Impostazioni* e poi seleziona image:icon_gear.gif["Icona delle azioni"] accanto al protocollo FC.
. Seleziona *Abilita FC* e inserisci l'indirizzo IP e la subnet mask per l'interfaccia di rete. + Le porte FC vengono assegnate automaticamente.
. Selezionare *Salva*.


|===
--
.CLI
--
. Abilitare il servizio FC sulla SVM:
+
[source, cli]
----
vserver fcp create -vserver <vserver_name> -status-admin up
----
. Creare due LIFF per le SVM su ciascun nodo che serve FC:
+
** Per ONTAP 9.6 e versioni successive:
+
[source, cli]
----
network interface create -vserver <vserver_name> -lif <lif_name> -data-protocol fcp -service-policy default-data-fcp -home-node <node_name> -home-port <port_name> -address <ip_address> -netmask <netmask> -status-admin up
----
** Per ONTAP 9.5 e versioni precedenti:
+
[source, cli]
----
network interface create -vserver <vserver_name>_ -lif <lif_name> -role data -data-protocol fcp -home-node <node_name> -home-port <port_name>
----


. Verificare che i file LIF siano stati creati e che il loro stato operativo sia `online`:
+
[source, cli]
----
network interface show -vserver <vserver_name> <lif_name>
----
+
Ulteriori informazioni su `network interface show` nella link:https://docs.netapp.com/us-en/ontap-cli/network-interface-show.html["Riferimento al comando ONTAP"^].



--
====
.Informazioni correlate
link:manage-lifs-all-san-protocols-concept.html["Scopri di più sulle LIF di ONTAP per ambienti SAN"].
